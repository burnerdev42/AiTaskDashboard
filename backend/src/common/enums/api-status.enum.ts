/**
 * @file api-status.enum.ts
 * @description Standardized API response status strings.
 * @responsibility Consistent feedback for the frontend.
 */

/**
 * Standardized status for all API responses.
 */
export enum ApiStatus {
  SUCCESS = 'success',
  FAILED = 'failed',
}
