/**
 * @file task-type.enum.ts
 * @description Defines categories of tasks within the innovation workflow.
 * @responsibility Provides a type-safe way to categorize tasks.
 */

/**
 * Categories of operational tasks.
 */
export enum TaskType {
  EVALUATION = 'evaluation',
  PILOT = 'pilot',
  VALIDATION = 'validation',
  STANDARD = 'standard',
}
