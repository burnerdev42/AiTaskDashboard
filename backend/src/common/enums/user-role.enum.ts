/**
 * @file user-role.enum.ts
 * @description Defines the access roles for users in the system.
 * @responsibility Provides a type-safe way to manage user permissions.
 */

/**
 * Roles for system access and permissions.
 */
export enum UserRole {
  ADMIN = 'Admin',
  USER = 'User',
}
